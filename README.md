```
mkdir P5Typescript

cd P5Typescript

npm init -y

npm install p5 --save

npm install webpack webpack-cli webpack-dev-server typescript ts-loader html-webpack-plugin @types/p5 css-loader style-loader node-sass sass-loader papaparse --save-dev
```